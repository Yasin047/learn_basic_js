const str = "  My name is yasin  ";
const accessTypeOne = str[1];
const accessTypeTwo = str.charAt(3);
const accessTypeThree = str.at(8);

// const slice = str.slice(3, 5);
// const subString = str.substring(3, 5);
// const subStr = str.substring(3, 7);

const indexOf = str.indexOf("y");
const lastIndexOf = str.lastIndexOf("y");
const startsWith = str.startsWith("M");
const endsWith = str.endsWith("n");
const includes = str.includes("z");
const search = str.search("y");
const match = str.match("m");

const length = str.length;
const toString = str.toString();
const concat = str.concat(" mahmud");
const split = str.split(" ");
const repeat = str.repeat(4);
const replace = str.replace("yasin", "Mazid");
const replaceAll = str.replaceAll("n", "x");
const toUpperCase = str.toUpperCase();
const toLowerCase = str.toLowerCase();
const trim = str.trim();
const trimStart = str.trimStart();
const trimEnd = str.trimEnd();
// const padStart = str.padStart(5, "x");
// const padEnd = str.padEnd();
// const localeCompare = str.localeCompare();

// for (let i = 0; i < str.length; i++) {
//   console.log(i, str[i]);
// }

// for (let key in str) {
//   console.log(key, str[key]);
// }

// for (let key of str) {
//   console.log(key);
// }

let i = 0;

// while (i < str.length) {
//   console.log(i, str[i]);
//   i++;
// }

// do {
//   console.log(i, str[i]);
//   i++;
// } while (i < str.length);
